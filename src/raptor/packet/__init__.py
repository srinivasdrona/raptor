"""PRD-04 Task A — the candidate-evidence-packet core.

Package marker only (no side effects). See `model.py` (schema), `config.py`
(frozen config loaders), `build.py` (`build_packet`), `direction.py`
(production candidate-direction policy) and `hashing.py` (the four
canonical packet hashes).
"""
