# Test-author task

Author executable RED acceptance tests in `tests/` from `SPEC.yaml` and the
real-shape fixtures in `artifacts/`. Do not implement `src/solution.py`.

Your suite must pass a correct implementation, fail meaningful contract
mutants, assert exact error codes and return shape, avoid vacuous assertions,
and leave the starter source unchanged.
