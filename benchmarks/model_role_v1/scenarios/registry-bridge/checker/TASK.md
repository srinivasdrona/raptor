# Checker task

Review the candidate implementation and tests against `SPEC.yaml`. Do not edit
them. Run any bounded probes needed, then write `VERDICT.yaml` conforming to
`OUTPUT_SCHEMA.yaml`. A CLEAN verdict is permitted only if no material defect
remains.
