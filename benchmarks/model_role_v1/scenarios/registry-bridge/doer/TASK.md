# Doer task

Implement `src/solution.py` against `SPEC.yaml`. Do not modify or delete the
visible tests or fixtures. Use only the Python standard library. Run the visible
tests and leave the workspace in a reproducible state.
