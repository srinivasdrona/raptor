# Checker task

Review the candidate implementation and visible tests against `SPEC.yaml`. Do
not edit them. Use injected resolver probes rather than requiring privileged
link creation. Write `VERDICT.yaml` conforming to `OUTPUT_SCHEMA.yaml`.
