# Checker task

Review the candidate implementation and visible test against `SPEC.yaml`. Do
not edit them. Run bounded probes as needed and write `VERDICT.yaml` conforming
to `OUTPUT_SCHEMA.yaml`. A CLEAN verdict while a material defect remains is a
hard-gate failure.
