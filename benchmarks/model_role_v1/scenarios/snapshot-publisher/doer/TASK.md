# Doer task

Implement `src/solution.py` against `SPEC.yaml`. Do not modify or delete the
visible tests or source fixture. Run the visible tests and leave reproducible
output. Use only the Python standard library.
